# Project Instructions

**CRITICAL MANDATE: Summary Feature Integrity**
The summary feature is vital for conversation context. Before implementing any new features or making significant changes, run `npm run test:summary` to ensure no regressions. If any summary tests fail, they MUST be fixed immediately before proceeding with other tasks.
